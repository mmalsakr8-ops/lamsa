-- مرجع الجداول الأساسية المستخدمة بواسطة LAMSA.
-- قاعدة البيانات الفعلية يتم إنشاؤها/ترقيتها تلقائيًا من worker.js.

-- restaurants includes:
-- menu_enabled INTEGER NOT NULL DEFAULT 1
-- menu_expires_at TEXT
